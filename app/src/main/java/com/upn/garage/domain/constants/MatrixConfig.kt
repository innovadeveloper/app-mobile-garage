package com.upn.garage.domain.constants

class MatrixConfig {
    companion object{
        var itemWidthSize = 100
        var itemHeightSize = (itemWidthSize * 2)
        var itemGaping = 10
        var factorSize = 8;
    }
}