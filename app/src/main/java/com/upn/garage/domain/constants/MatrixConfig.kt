package com.upn.garage.domain.constants

class MatrixConfig {
    companion object{
        var itemWidthSize = 100 * 2
        var itemHeightSize = (itemWidthSize * 3)
        var itemGaping = 10
        var factorSize = 8;
    }
}